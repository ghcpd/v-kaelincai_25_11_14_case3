Project B — Improved UI

How to run:
1. Create and activate a Python venv: `python -m venv .venv` and `. .venv/Scripts/activate` (PowerShell: `. .\.venv\Scripts\Activate.ps1`)
2. `pip install -r requirements.txt`
3. `playwright install`
4. Run tests: `.venv\Scripts\activate` then `pytest -q`

Artifacts generated: screenshots/screenshot_post_1.png, results/results_post.json, logs/log_post.txt

Design improvements: CSS variables, spacing system, unified button components, hover and focus states, accessible typography.
